---
interface Props {
  class?: string;
  delay?: number;
}
const { class: className = '', delay = 0 } = Astro.props;
---
<div class:list={['reveal', delay > 0 && `reveal-delay-${Math.min(Math.round(delay * 10), 5)}`, 'overflow-hidden', className]}>
  <slot />
</div>
