---
interface Props {
  label: string;
  class?: string;
}
const { label, class: className = '' } = Astro.props;
---
<p class={`font-[family-name:var(--font-mono)] text-[10px] tracking-[0.25em] text-bone/30 uppercase ${className}`}>
  {label}
</p>
